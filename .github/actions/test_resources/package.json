{
  "name": "repo-resources-js",
  "version": "1.0.0",
  "main": "dist/main.js",
  "scripts": {
    "build": "ncc build src/main.js -o dist && ncc build src/post.js -o dist"
  },
  "dependencies": {
    "@actions/core": "^1.10.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.0"
  }
}
